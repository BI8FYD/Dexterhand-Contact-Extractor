[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "dexterhand-contact"
version = "0.1.0"
description = "Crop DexterHand demonstrations and extract five fingertip-to-Cuboid contacts."
readme = "README.md"
requires-python = ">=3.10"
license = {file = "LICENSE"}
authors = [{name = "DexterHand Contact contributors"}]
dependencies = ["numpy>=1.23", "scipy>=1.9", "tqdm>=4.0"]

[project.optional-dependencies]
mano = ["torch", "smplx"]
viewer = ["rerun-sdk"]
dev = ["pytest"]

[project.scripts]
dexterhand-contact = "dexterhand_contact.cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
pythonpath = ["src"]

[tool.setuptools.packages.find]
where = ["src"]
